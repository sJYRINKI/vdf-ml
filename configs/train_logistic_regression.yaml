dataset_dir: data/train/timesteps_{dataset_id}
output_dir: data/models/logistic_regression/{model_id}

features:
  downsample_factor: 8
  batch_size: 512
  n_jobs: 4

model:
  max_iter: 1000
  penalty: l2
  solver: lbfgs
  C: 1.0
