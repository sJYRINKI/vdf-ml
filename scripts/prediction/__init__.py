"""Contain thin coordinate and bounded-region prediction commands.

These command modules translate CLI choices into current prediction
configuration overrides and delegate source access, inference, output, and
optional figures to ``src.prediction``.
"""
