"""Contain thin spatial-colormap and peak-centred VDF plotting commands.

Both commands parse saved-dataset selections and output paths, then delegate
figure construction and resource ownership to ``src.plotting``.
"""
