"""Contain the eight thin module entry points for current workflows.

Each command parses CLI arguments, loads at most one YAML mapping, delegates
scientific work to :mod:`src`, and prints a concise result. Run these
modules from the repository root with ``python -m``.
"""
