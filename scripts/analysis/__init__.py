"""Contain the thin label-blind PCA analysis command.

The command boundary parses dataset, representation, configuration, and
output paths before delegating every numerical stage to
``src.analysis``.
"""
